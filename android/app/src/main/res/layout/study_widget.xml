<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/widget_streak"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/widget_sign_in"
        android:textColor="#FFFFFFFF"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:text="@string/widget_study_prompt"
        android:textColor="#FFB7B9CB"
        android:textSize="12sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        android:layout_weight="1"
        android:gravity="bottom"
        android:orientation="horizontal">

        <TextView android:id="@+id/widget_quick_study" style="@style/WidgetAction" android:text="@string/widget_quick_study" />
        <TextView android:id="@+id/widget_global_chat" style="@style/WidgetAction" android:layout_marginStart="6dp" android:text="@string/widget_global_chat" />
        <TextView android:id="@+id/widget_new_notebook" style="@style/WidgetAction" android:layout_marginStart="6dp" android:text="@string/widget_new_notebook" />
    </LinearLayout>
</LinearLayout>
